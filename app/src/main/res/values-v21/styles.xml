<resources>

    <!-- Base application theme. -->
    <style name="Theme.XposedManager" parent="Theme.XposedManager.Base">

        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
